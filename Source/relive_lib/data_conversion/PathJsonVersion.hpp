#pragma once

#include "../Types.hpp"

// Version of the editor's path json format (its "path_version" field).
// Increment when a breaking change to the json is made.
constexpr s32 kPathJsonVersion = 4;
